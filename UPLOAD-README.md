# M1.5 产品化增量上传说明

本目录是 **T16 已上传完成** 前提下的 M1.5 增量包。

## 上传方式

把本目录中的文件按原有相对路径覆盖到仓库根目录：

- `index.html` → 仓库根目录 `index.html`
- `src/...` → 仓库 `src/...`
- `tests/...` → 仓库 `tests/...`
- `docs/...` → 仓库 `docs/...`

不要删除仓库中的其他文件。本包不包含 `node_modules` 和 `dist`，线上构建应由仓库源码重新执行。

## 本批内容

1. 全局产品化视觉基线与新版顶栏。
2. 左栏、右栏、底栏可收纳且刷新后保留偏好。
3. 属性与打印检查分成右侧页签。
4. 资产搜索和最近/名称/面数排序。
5. 空场景 AI / 导入双入口。
6. M1.5 产品化路线与验收文档。

## 本地验证结果

- 自动化测试：`368 passed`
- TypeScript：通过
- 生产构建：通过
- 外部 Chrome：收纳、偏好保存、资产搜索排序、打印检查切页均通过
