# M1.14b.1 UI 记录

| 项目 | 内容 |
| --- | --- |
| 日期 | 2026-08-01 |
| 版本 | Agent 结构化输出兼容修复 |
| 当前状态 | 已部署；故障截图保留为诊断证据，更换 Secret 并改用专用 API 主机后服务已恢复 |
| 测试环境 | Cloudflare 生产环境，桌面浏览器 |

## 截图清单

| 文件 | 证据 |
| --- | --- |
| `01-production-upstream-failure.png` | M1.14b 生产环境中，对话降级为本地引导、视觉方案失败并返还额度的真实故障状态 |

## 体验结论

- 用户操作路径和 Brief 确认状态正常；
- 失败发生在点击“生成 3 套视觉方案”后的服务调用阶段；
- 页面没有伪造方案，也没有继续调用生图或 3D 服务；
- M1.14b.1 保留现有布局，只升级错误诊断与接口兼容性，因此该版本不单独伪造交互截图；
- 部署后的服务端诊断已把通用失败细分为 AIHubMix 401 授权错误，前端仍安全降级并返还额度；
- 后续生产修复已验证真实 LLM 对话与三方案均返回 HTTP 200；本目录继续保留原故障态，成功态归入后续 Agent 版本验收截图。
