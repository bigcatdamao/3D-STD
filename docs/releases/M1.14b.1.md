# 3D Studio M1.14b.1 开发记录

| 项目 | 内容 |
| --- | --- |
| 日期 | 2026-08-01 |
| 状态 | 修复候选版；自动化验证完成，待生产联调 |
| 上一版本 | M1.14b |
| 主题 | 创作 Agent 与视觉方案结构化输出兼容修复 |
| UI 档案 | `docs/ui-history/M1.14b.1/` |

## 用户问题与判断

生产截图显示两条 LLM 链路都发生了安全降级：对话消息标记“本地引导 · 未调用大模型”，三套视觉方案请求失败并返还额度。该现象不是用户输入或操作错误，而是第三方兼容接口未接受本轮结构化输出请求。

第一轮修复优先处理最可能的兼容差异：把两份输出 Schema 收敛到更保守的 JSON Schema 子集，同时为服务端增加不泄露响应正文的状态码和机器错误码诊断。

## 修改内容

- 移除请求 Schema 顶层 `$schema`；
- 用单值 `enum` 替代 `const`；
- 移除不影响运行时校验的字符串长度约束；
- 使用 `type: [T, "null"]` 表达可空字段；
- 保留根对象、必填字段和 `additionalProperties: false` 的严格输出边界；
- 对第三方失败只提取 HTTP 状态、`error.code` 和 `error.type`，不记录响应正文；
- 区分结构不兼容、授权失败、限流、超时与一般服务失败；
- 所有失败仍返还本轮应用内额度，且不生成本地假方案；
- 页面版本标识升级为 `M1.14b.1`。

## 安全边界

- API Key 仍仅从 Cloudflare Secret 读取；
- 前端、Git、日志和浏览器响应均不返回密钥；
- 不透传第三方错误正文，避免服务商信息、请求内容或敏感字段进入日志；
- 生产验证只检查状态、Provider、模型与结构数量，不输出用户内容或完整模型响应。

## 验收门槛

- [x] TypeScript 类型检查通过；
- [x] 两条接口的定向测试覆盖保守 Schema 与安全错误分类；
- [x] 全量测试通过：72 个测试文件、539 项测试；
- [x] 生产构建通过：710 个模块完成转换；
- [ ] 生产“需求对话”返回真实 LLM 结果；
- [ ] 生产“3 套视觉方案”返回真实结构化方案；
- [ ] Git 提交、远程上传和生产部署记录补齐。

## 回滚

如第三方兼容性仍不满足，前端会继续使用本地引导并明确标注“未调用大模型”；视觉方案失败会退款且不展示伪结果。可回滚到 M1.14b 的生产提交，不影响原有导入、编辑、检查、导出与 Hi3D 链路。
